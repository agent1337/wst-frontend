import { danger, main, dark_grey } from "colors";

export const styles = {
  input: {
    border: `1px solid ${dark_grey}`,
    borderRadius: "5px",
    height: "40px",
    paddingLeft: "10px",
    marginBottom: "23px",
    outlineColor: `${main}`,
  },
  require: {
    color: `${danger}`,
  },
};
