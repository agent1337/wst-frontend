import { main } from "../../../colors";

export const styles = {
    input: {
        width: 'calc(100% - 16px)',
        borderRadius: "3px",
        padding: "10px 8px",
        outlineColor: `${main}`,
        marginBottom: '20px'
    },
}